

#include <math.h>

typedef unsigned int uint;
radixsort

uint mask = pow(2,size_base_bits) - 1;
size_t histo_size = sizeof(int) * 8 / size_base_bits;
int *histo = calloc(histo_size, sizeof(int));

/*fill histo*/
/*shift mask size_base_bits to the left:*/
        mask <<= size_base_bits;






fillhisto(mask, histo, histo_size, arr, arr_size)
{
    /*go over arr.
     * for every item, save in a variable,
     * make a new variable for the number that is that number and the mask - thats the index
     * increment value in that index in histo*/
}
